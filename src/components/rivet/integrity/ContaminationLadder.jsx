import React from 'react';
export default function ContaminationLadder({ quality }) {
  if(!quality)return <p className="text-xs text-muted-foreground">No rung-based contamination assessment exists for this legacy version.</p>;
  const color={pass:'text-emerald-400',flag:'text-amber-400',manual:'text-sky-400',not_applicable:'text-muted-foreground'};
  return <details className="rounded-lg border border-border p-3 text-xs"><summary className="cursor-pointer font-semibold">Contamination checks · {quality.scanStatus === 'complete' ? 'corpus scan complete' : 'scan incomplete or legacy'}</summary><p className="mt-2 text-muted-foreground">{quality.checkedPeerCount ?? 'Unknown'} / {quality.eligiblePeerCount ?? 'unknown'} peer snapshots checked · {quality.failedPeerCount ?? 'unknown'} unreadable. Corpus coverage does not clear unresolved manual checks or prove absence of training exposure.</p><div className="mt-3 space-y-2">{quality.checks.map(check=><div key={check.rung} className="border-t border-border pt-2"><p><strong>R{check.rung} · {check.name}</strong> <span className={color[check.status]}>· {check.status.replace('_',' ')}</span></p><p className="text-muted-foreground">{check.detail}</p></div>)}</div><p className="mt-3 text-muted-foreground">{quality.scope}</p></details>;
}